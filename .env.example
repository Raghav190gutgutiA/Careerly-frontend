# Left unset by default - the app derives the backend URL from whatever host the page was
# loaded from (see src/utils/constants.js). Set these only for a production deployment where
# frontend/backend live on different domains.
# VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
# VITE_SOCKET_URL=https://api.yourdomain.com
